import errorHandler from '../errorHandler';

describe('errorHandler', () => {
  it('should be a function', () => {
    expect(typeof errorHandler).toBe('function');
  });

  /**
   * @TODO implement test
   */
  it.skip('return correct status', () => {});
  it.skip('return correct message', () => {});
});
